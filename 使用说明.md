# 自定义桌宠启动器

## 启动流程

双击 `DesktopPet.exe` 或 `启动桌面小人.cmd`，**首先打开启动器，不会直接生成桌宠**。

1. 点击顶部「设置 API / 模型」，设置 AI 接口；也可以先跳过，离线体验桌宠。
2. 从「我的皮肤」选择现有角色，点击「编辑角色」修改已有角色，或点击「新建角色」添加角色。
3. 新建和编辑都在原窗口切换到角色编辑页。「状态图片」配置主图、表情、动作；「角色人设」配置称呼、性格、说话方式、背景和喜好。
4. 点击「创建并返回」或「保存并返回」，再在主页点击「启动桌宠」。此时桌宠才出现在桌面，启动器隐藏。

运行时右键桌宠 →「打开启动器 / 更换皮肤…」，即可再次打开配置界面。重新运行 EXE 也会唤起已有启动器，不会重复创建实例。

## 新建角色与上传图片

点击「新建角色」，原窗口切换到空白角色编辑页；选中已有角色后点击「编辑角色」，同一页面会载入该角色现有图片和完整人设。两者均不弹出新窗口。填写角色名称后，在「状态图片」标签中选择左侧状态并点击「上传图片」。选择文件仍使用系统文件选择框，支持 PNG、JPG/JPEG 或 BMP。

| 图片位置 | 是否必填 | 作用 |
| --- | --- | --- |
| 主图 / 默认站姿 | **必填** | 桌宠正常站立、待机时的完整角色图片 |
| 开心、眨眼、惊讶、难过、生气 | 选填 | 对应互动表情 |
| 走路、跑步、跳跃、挥手 | 选填 | 对应动作时显示的完整角色图片 |

每个状态使用一张完整角色图片。上传后可以单独预览、替换、移除或选择去背景；未上传的表情和动作自动使用主图，同时保留移动、跳跃等行为。主图、角色名称和人设称呼齐全才能保存；新角色的称呼默认跟随角色名称，可在「角色人设」中单独修改。创建或保存成功后返回主页，选中相应角色；点击「启动桌宠」才会在桌面显示。点击「取消并返回」会丢弃本次未保存修改，新角色不会写入目录，已有角色也不会改变。关闭程序不会自动保存角色编辑页中的草稿。

- **推荐透明 PNG**：原有透明区域和半透明边缘会保留。
- 普通图片默认保留背景。每个图片状态可单独勾选「去除当前图片的边缘纯色背景」，去除与左上角近似同色且连接边缘的部分，先检查预览再创建。该选项只适合简单纯色背景；复杂背景需要先自行抠图。
- 图片最大 16 MB，宽高不超过 8192，总像素不超过 16,777,216。导入时按原比例缩小到最长边不超过 512 像素，保存为 PNG。
- 图片复制到程序内的独立角色目录，之后移动或删除原图片不影响桌宠；导入操作在本机完成。
- 每次导入创建独立目录，即使皮肤名称相同，也不会覆盖已有角色。
- 只上传主图也可使用全部基础互动；上传了表情和动作图，则在相应状态切换显示。当前每个状态一张图，通过位移和翻转呈现动作，**不会自动生成逐帧动画**。已有角色包继续兼容。
- 横向宽图会按比例缩小到桌宠画布内，避免裁切或拉伸。

新角色保存在 `characters/custom_<唯一编号>/`，包含必需的 `normal.png`（主图）、已上传的 `happy.png`、`run.png` 等状态图，以及 `character.json` 和 `persona.json`。选填状态缺图时直接引用主图，不重复保存图片。旧版使用 `skin.png` 的角色包仍可正常使用和编辑。编辑已有角色时，新图片与人设使用独立文件名保存，最后一次性更新 `character.json` 的引用；原素材保留，便于恢复。人设实际路径以 `character.json` 中的 `persona` 字段为准。

## 皮肤与人设绑定

每套皮肤绑定一份独立人设。主页只显示角色列表、预览、编辑入口和启动操作，不直接显示人设输入框。选择角色并点击「编辑角色」，然后切换「角色人设」标签，即可查看和修改该角色全部人设字段。

- 「称呼」是 AI 使用的名字；皮肤列表名称来自可编辑的「角色名称」。
- 「性格与角色要求」可以写角色身份、性格、互动习惯；「说话方式」可以指定口吻、用词和回答长度。
- 称呼最多 64 字，性格和说话方式各最多 2,000 字，背景最多 4,000 字。喜好每类最多 30 项、每项最多 128 字，每行一项。
- 编辑页点击「保存并返回」，图片配置和人设一起保存到原角色；不会新建一个重复角色。点击「取消并返回」丢弃修改。保存失败会停留在编辑页并保留输入，原角色配置保持完整。
- 正在运行时选择另一套皮肤后，点击「应用并切换桌宠」，才会替换桌面上的角色。上一套角色的窗口和未完成请求会关闭，新角色使用新的聊天上下文。
- 保存当前运行角色的人设后立即切换设定，清除旧人设的临时上下文和气泡，并取消尚未完成的旧请求；迟到的旧回复会丢弃。历史记录仍保留。手动修改文件时，运行中的桌宠约每 2 秒检查一次，每次发消息也会重新读取人设。
- 启动成功会记住所选皮肤；下次打开程序仍先进入启动器，不会自动启动桌宠。

人设通过系统提示词发送给用户配置的文字模型。当前使用的接口每次请求都携带人设，因为它不会自动记住上一次请求的系统设定。这部分会计入输入 token；写清身份、语气和关键约束即可，不必重复堆砌描述。性格、背景、说话方式会共同发送，互相矛盾的要求可能影响效果。模型是否严格遵循语气取决于其能力，界面不会伪造模型回复。

## 聊天记录与上下文

- AI 聊天窗口展示双方发言和时间，支持滚动和「更早记录」。也可从启动器「查看聊天记录」或桌宠右键「聊天记录…」进入。
- 记录按角色分别保存在程序旁的 `user-data/history/`，关闭聊天窗口、收回桌宠或重启程序后仍可查看。失败和取消会保留用户发言并标注结果，不伪装成 AI 回复。旧版本未保存的对话无法补回。
- 本地存档和发送给模型的上下文相互独立。默认只携带当前会话最近 **2 轮**完整问答，可以切换为不带历史、2、4、6 轮；历史文本还受 6,000 字符预算限制（字符不是 token）。人设和当前问题始终发送，不会将整个存档上传。
- 重新启动桌宠、切换模型或修改人设后，开始新的临时上下文；旧记录仍可阅读。选择「不带历史」能节省输入 token，但模型无法关联前后问题。
- 聊天正文以本地文本保存；分享程序时请去掉 `user-data` 文件夹。

## 符合人设的日常短句

程序使用当前 API 和模型，一次生成一组待机短句及摸头、挥手、跑步等互动台词，保存在该角色目录的 `ambient-lines.json`。播放这些短句只读取本地缓存，不请求模型，也不消耗 token。

新建或保存角色、启动桌宠、保存 API 配置时，会检查短句缓存。只有没有可用缓存或人设内容变化时才自动生成；已有有效缓存时，仅改图片、自定义台词、重复保存或重启不会重复调用。人设修改后旧的模型短句立即失效，后台生成新版；期间再次修改人设，旧生成结果也不会生效。启动器「重新生成短句」可主动重做一次。

在「编辑角色 → 默认台词」中可以自己填写台词，新建角色时也有相同的标签页。优先级始终为 **用户自定义 > 当前人设的模型生成台词 > 初始设定**：

- 互动台词逐项回退。例如只填写摸头台词，摸头就固定使用自定义内容，挥手等未填写项目仍使用模型生成或初始台词。
- 待机台词每行一句，最多 20 句；有填写时只从这些句子中随机选用，全部清空后恢复使用模型的待机短句。每句最多 40 字符。
- 保存后，运行中的桌宠立即使用新台词；取消编辑不会保存。修改人设和重新生成模型台词均不会覆盖用户自定义内容，用户需要自行调整与新人设不符的自定义台词。
- 自定义内容保存在角色的 `character.json` 内的 `custom_dialogue` 字段。角色包原有的 `messages` 保留为初始设定，缺少对应初始台词时使用程序内置短句。

没有 API、联网失败或模型返回格式错误时，自定义与初始台词仍可使用，启动器显示状态；不会在待机时不断联网重试。配置完成后或点击重新生成即可更新。一次生成本身会消耗输入和输出 token，本地播放台词不消耗 token。初始台词不保证符合后来修改的人设。

正常待机约每 30～50 秒显示一句，正在聊天或开启「安静陪伴」时不插话。聊天窗口关闭后，最后的 AI 气泡约 60 秒后自动收起，记录仍保留。

## API 设置

沿用原有 API 功能和加密存储。可从启动器右侧「AI 连接」卡片设置，也可通过桌宠右键「设置模型…」打开。

| 配置项 | 内容 |
| --- | --- |
| 接口地址（Base URL） | 服务商提供的 OpenAI-compatible API 基础地址；例如 `https://api.openai.com/v1`。程序自动追加 `/chat/completions`，已填完整地址则不重复追加。 |
| API Key | 服务商密钥，无需加 `Bearer`。默认隐藏，勾选「显示密钥」可查看。 |
| 选择模型 | 点击「获取模型列表」后选择文字聊天模型，也可以手动输入模型 ID。 |
| 等待超时 | 1～180 秒，默认 45 秒。 |

目前支持 OpenAI-compatible Chat Completions 文字接口。本机 `localhost` / `127.0.0.1` 免密服务可以不填 Key，其他地址需要填写密钥。

打开设置窗口不主动联网；「获取模型列表」会请求服务商 `/models` 接口。列表不可用时仍可手动输入模型 ID 保存。保存会校验配置，并为当前角色检查、补生成缺失或过期的日常短句。

API 设置由所有皮肤共用，保存在程序旁 `model-settings.json`。密钥使用 Windows 当前用户保护功能加密，不进入角色包。换电脑或 Windows 用户后需重新填写密钥；分享程序时不要附带自己的 `model-settings.json`、`.env` 或聊天日志。

兼容旧 `.env` 和 `AI_*` 环境变量；界面保存后的设置优先。重新打开配置窗口会回填已保存内容。没有 API 配置也能启动桌宠，只有聊天时需要配置。

## 桌面操作

| 操作 | 效果 |
| --- | --- |
| 按住桌宠拖动 | 移动位置 |
| 单击 / 双击 | 摸摸头 / 跳一下 |
| 右键 → AI 聊天… | 打开文字输入框；Enter 发送，Shift+Enter 换行 |
| 右键 → 打开启动器 / 更换皮肤… | 返回配置界面，桌宠继续运行 |
| 启动器 → 收回桌宠 | 关闭当前桌宠，保留启动器 |
| 右键 → 收回桌宠（返回启动器） | 关闭当前桌宠并显示启动器 |
| 关闭启动器 | 退出整个程序，同时关闭桌宠、聊天窗口和托盘图标 |
| 双击通知区域的桌宠头像 | 找回桌宠到鼠标所在屏幕右下角 |
| 取消自动散步 / 勾选安静陪伴 | 停止散步 / 停止自动动作和待机起伏 |
| 气泡上滚动鼠标滚轮 | 翻页查看长回复 |

桌宠透明区域可以点击后方窗口，拖拽和动画不会因等待 AI 回复而停止。失败、取消的请求不会进入模型的临时上下文，但会在本地聊天记录中标注。

## 文件与开发

界面采用浅蓝背景、白色圆角卡片和青蓝色按钮。启动器左侧管理角色，中间预览和编辑，右侧配置 AI、生成短句及查看聊天记录。新建与编辑仍在同一窗口切换，包含「状态图片」「角色人设」「默认台词」三个标签；API 设置、聊天、独立历史记录、右键菜单和桌宠气泡使用同一配色。窗口变矮时欢迎横幅会收紧，表单内容可滚动。

主页进入新建／编辑页、返回主页，以及编辑页标签之间的切换，使用约 180 毫秒的轻微滑动与淡入。返回方向与进入相反；连续切换以最后一次选择为准。调整窗口大小、隐藏或关闭窗口会立即结束过渡。动画仅影响窗口内的页面内容，不改变整个窗口透明度；Windows 关闭客户端区域动画时，程序也会跳过这些效果。

样式和原生绘制组件集中在 `src/UiTheme.cs`。爪印、云朵和预览背景由程序绘制，不依赖网络素材。界面预览保存在 `tests/results/ui-*.png`，覆盖主页、空角色列表、新建、编辑的三个标签、API 设置、聊天等待状态和聊天记录；预览使用隔离测试数据，不会读取真实密钥或发送模型请求。

页面过渡由 `src/PageTransition.cs` 管理，临时画面与计时器在结束后释放。`tests/TransitionTests.cs` 覆盖实际中间帧、连续切换、窗口缩放、关闭清理与重复导航的图形资源检查。

程序使用 Windows .NET Framework 4.5+，无需 Python 或 Node.js。移动程序时请保留 `DesktopPet.exe`、`characters` 目录及 `runner.json`。程序目录需要可写，以保存图片、人设和配置。

| 文件 | 职责 |
| --- | --- |
| `src/LauncherForm.cs` | 启动器主页、角色选择与预览、页面切换、桌宠生命周期 |
| `src/CharacterEditorPage.cs` | 新建与编辑共用的窗口内页面，状态图片与完整人设配置 |
| `src/Characters/CharacterLibrary.cs` | 图片处理、角色导入、角色列表、原子保存人设和默认选择 |
| `src/Characters/CharacterLoader.cs` | 角色格式与素材校验、透明图片读取 |
| `src/DesktopPet.cs` | 程序入口、透明桌宠、互动和托盘菜单 |
| `src/ModelSettingsForm.cs` | API 设置、模型列表、手动模型输入 |
| `src/Chat/` | 配置、加密、模型请求、人设提示词、聊天上下文和日志 |
| `src/Chat/ChatArchive.cs` | 按角色保存双方发言，与模型上下文分开 |
| `src/Chat/AmbientDialogue.cs` | 生成人设短句、校验并缓存、人设变化使缓存失效 |
| `tests/LauncherTests.cs` | 图片导入、角色绑定、启动流程、切换与请求取消的自动测试 |
| `tests/ConversationFeatureTests.cs` | 历史持久化、上下文预算、人设即时切换、短句缓存与过期生成丢弃 |

编译前关闭正在运行的启动器和桌宠：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
```

回归测试：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\run-tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\run-chat-tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tests\run-smoke.ps1
```

聊天测试使用隔离的临时角色和本地模拟接口，不读取真实 API 密钥，也不调用收费模型。覆盖先启动器后桌宠、导入后原图可删除、透明度、纯色去背景、宽图比例、人设独立保存、切换时上下文隔离、关闭与重启、原有聊天和动画。独立进程测试验证重复启动及真实启动按钮。

测试报告和界面截图在 `tests/results/`，启动器截图为 `launcher-window.png`，新建角色页截图为 `create-character-window.png`，编辑图片页为 `edit-character-window.png`，人设页为 `edit-persona-page.png`。新增测试覆盖主图必填、选填回退、独立动作渲染、替换失败保留原图，取消创建不写入文件、取消编辑保持原角色、同窗口页面切换、旧角色素材保留，以及保存失败的回滚。原 demo 测试使用独立指定的示例角色，不要求更改用户已选择的角色。

如果角色包损坏，启动器会列出其他可用角色并显示错误；默认选择文件丢失时仍可进入启动器导入或选择皮肤。图片或人设保存错误会显示在界面底部。聊天错误日志在 `%LOCALAPPDATA%\DesktopPet\logs\chat.log`，不记录密钥或聊天正文；启动异常记录在 `%TEMP%\PixelCompanion-error.log`。

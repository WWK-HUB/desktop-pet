# 角色包使用说明

桌宠程序负责窗口、动作、气泡和 API 调用；角色目录负责素材、显示信息和人设。换角色不需要改 C# 代码或重新编译。

## 角色包目录示意

公开仓库不附带私人角色和图片。首次使用请通过启动器“新建角色”导入自己的素材；下列目录仅为格式示意。

```text
DesktopPet.exe
runner.json                       # 默认角色选择
model-settings.json               # 现有 API 设置，共用，不要分享
characters/
  demo_character/
    character.json                # 运行配置
    persona.json                  # 人物设定
    assets/
      character-sheet.png         # 你自己的角色设定图
```

`tests/fixtures/` 中的配置和脚本生成的几何图片仅用于测试，不包含作者的私人素材。你在 `characters/` 中创建的角色和根目录的默认选择 `runner.json` 均不会提交到 Git。

## character.json：桌宠如何显示

打开现有包的文件即可看到完整例子。JSON 不支持注释，最后一项后面不能加逗号。

| 字段 | 含义 |
| --- | --- |
| `schema_version` | 当前固定为 `1`，供以后升级格式 |
| `id` | 角色唯一标识，1～64 位英文字母、数字、下划线或短横线 |
| `display_name` | 菜单、窗口和托盘中的名称，1～30 字符 |
| `default_size` | 默认小人绘制高度，80～300 像素 |
| `size_options` | 大小菜单的选项，最多 6 个，必须包含默认尺寸 |
| `default_expression` | 默认待机表情，对应 `expressions` 的一个键 |
| `default_animation` | 启动时的动作，对应 `animations` 的一个键 |
| `persona` | 人设文件的相对路径，一般为 `persona.json` |
| `icon` | 托盘图标所用的素材名，可省略，此时使用默认表情 |
| `sprite_sheet` | 共用设定图的相对路径；全部用独立图片时可以省略 |
| `remove_dark_background` | 是否将接近黑色的背景去除；透明 PNG 应设为 `false` 或省略 |
| `background_threshold` | 去黑底阈值，0～255；当前原图使用 38 |
| `sprites` | 素材名与图片路径、裁切区域的对应关系，1～64 项 |
| `expressions` | 表情状态名与素材名的对应关系 |
| `animations` | 动作状态名、素材名与运行器运动方式的对应关系 |
| `messages` | 摸头、拖动等固定互动短句，每条最多 40 字符，可省略 |

当前包通过 `region: [左边距, 上边距, 宽度, 高度]` 从原设定图裁切素材。坐标从图片左上角开始，单位是像素。加载器会去除裁切结果四周的透明空白，并检查区域是否超出图片。

也可以直接使用独立透明 PNG。例如下面的完整最小配置只需要 `expressions/normal.png` 和 `persona.json`：

```json
{
  "schema_version": 1,
  "id": "my_character",
  "display_name": "我的小伙伴",
  "default_size": 190,
  "size_options": [140, 190, 250],
  "default_expression": "normal",
  "default_animation": "idle",
  "persona": "persona.json",
  "remove_dark_background": false,
  "sprites": {
    "normal_image": { "path": "expressions/normal.png" }
  },
  "expressions": {
    "normal": "normal_image"
  },
  "animations": {
    "idle": { "sprite": "normal_image", "motion": "idle" }
  },
  "messages": {
    "startup": "你好呀！",
    "pet": "谢谢陪伴！"
  }
}
```

每个 `sprites` 条目的 `path` 优先于共用 `sprite_sheet`，未填 `region` 就使用整张图片。路径相对于当前角色目录，只能指向包内真实文件；图片每张最大 16 MB，宽高不超过 8192 且总像素不超过 16,777,216。

现有运行器的互动使用以下状态名：

| 类型 | 状态与作用 |
| --- | --- |
| 表情 | `normal` 普通、`happy` 开心、`wink` 眨眼、`surprised` 惊讶/拖动、`sad` 难过、`angry` 生气 |
| 动作 | `idle` 待机、`walk` 散步、`run` 跑步、`jump` 跳跃、`wave` 挥手 |

表情名和动作名不能重复。动作的 `motion` 目前支持 `idle`（轻微起伏）、`walk`（行走位移）、`run`（较快位移）、`jump`（跳跃位移）。挥手使用挥手姿势图配合 `idle` 即可。这沿用原 Demo 的单姿势位移动画，没有新增逐帧动画格式。

新角色可以只有一张图片。没有提供的可选状态会使用默认表情显示，不会因为缺少整套素材而崩溃。若希望完整保留原有互动效果，请按上表提供对应表情与动作。待机时使用 `default_expression`，启动时使用 `default_animation`。

## persona.json：AI 如何说话

```json
{
  "name": "小曜",
  "personality": "温柔、活泼、好奇，喜欢鼓励别人。",
  "speech_style": "用轻快亲切的口吻，以「嘿，」开头，使用一到两句短句。",
  "background": "一个原创的桌面小伙伴，陪用户学习和处理日常事务。",
  "likes": ["晒太阳", "陪伴学习"],
  "dislikes": ["催促", "嘲笑别人"]
}
```

`name` 必须填写；其他字段可以省略或留空。`likes` 和 `dislikes` 使用字符串数组。未知字段暂时忽略，便于以后扩展。`name` 最多 64 字符，性格和说话方式各最多 2,000 字符，背景最多 4,000 字符，喜好数组各最多 30 项、每项最多 128 字符。

运行流程：

```text
启动 → 启动器 → 选择角色并点击启动 → CharacterLoader 检查角色 → 桌宠窗口
聊天 → ChatService → CharacterPersonaPromptSource → CharacterLoader.LoadPersona
     → PersonaPromptBuilder → System Prompt + 最近对话 → 已配置的大模型 → 原有气泡
```

**在编辑页保存当前人设后立即生效。** 人设文件路径以 `character.json` 的 `persona` 字段为准；编辑页保存后可能指向新的版本文件。提示词改变时清空临时上下文，取消旧请求，丢弃迟到的旧回复；本地双方聊天记录不会删除。手动改文件时桌宠约每 2 秒检查一次，每次聊天也会重读。修改失败会在气泡中提示，修复文件后可以重试。角色配置、人设或必要素材缺失时会显示加载错误。

人设会随每次聊天请求发送；模型上下文默认限最近 2 轮并受 6,000 字符预算约束，本地完整记录不自动上传。人设各字段应保持一致，程序无法保证模型完全遵循所有要求。

`ambient-lines.json` 保存模型生成的日常短句，其中 `persona_hash` 绑定实际人设提示词内容，`messages` 存互动台词，`idle` 存待机台词。启动、编辑页保存或保存 API 配置时检查缓存，仅缺失或过期才自动生成；手动编辑人设文件后旧缓存会失效，可在启动器重新生成。相同人设的短句可无限次离线复用，不自动反复重试失败的请求。

默认台词优先级为 **用户自定义 > 模型生成 > 初始设定**。`character.json` 的 `custom_dialogue.messages` 按互动键保存用户台词，`custom_dialogue.idle` 保存用户待机句子数组。互动键未填或为空白时逐项回退；待机数组有有效内容时只选用用户提供的句子。每句最多 40 字符，待机最多 20 句。修改人设和重新生成仅更新模型缓存，不改写自定义内容。原 `messages` 保留为初始设定，其中可用 `idle` 指定初始待机短句；缺少初始值时使用程序内置短句。旧角色包无需增加新字段即可继续使用。

用户可在原窗口的角色编辑页「默认台词」标签修改这些内容，与图片、人设一同保存或取消。例如：

```json
"custom_dialogue": {
  "messages": { "pet": "收到你的摸摸啦。", "wave": "嗨，我在这里！" },
  "idle": ["休息一下吧。", "今天也陪着你。"]
}
```

语气对比可以使用同一个问题「今天学习有点累，给我一个小建议吧」。先使用轻快设定，再将 `speech_style` 改成「用正式简洁的口吻回答，以『建议：』开头，不使用语气词」，保存并再次发送相同问题。

以后扩展人设字段，在 `src/Characters/CharacterPack.cs` 的 `PersonaDefinition` 增加字段，在 `CharacterLoader.LoadPersona` 校验，再在 `src/Chat/PersonaPromptBuilder.cs` 转换为提示词即可。UI 和模型 Provider 都无需知道具体字段。

## 从启动器添加角色

点击「新建角色」在原窗口切换到创建页；已有角色点击「编辑角色」进入同一编辑页。必填角色名称和主图，选填开心、眨眼、惊讶、难过、生气、走路、跑步、跳跃和挥手图片。程序会创建 `characters/custom_<唯一编号>/`，自动写入各状态的图片映射和独立人设，无需手写 JSON。状态图片与人设都在编辑页配置，主页不放人设输入框；切换皮肤后点击「应用并切换桌宠」，运行中的桌宠才会替换。

主图保存为 `normal.png`，用作 `normal` 表情、`idle` 待机和图标；上传的其他图片保存为对应状态名称。没有上传的表情和动作映射到 `normal`，但动作仍保留自己的运动方式（如 `run`、`jump`）；`wave` 使用 `idle` 运动方式。透明 PNG 保留透明度；每张图片可单独选择去除边缘纯色背景。已有单图角色包保持兼容。

## 手动添加多表情角色包

1. 在启动器中新建角色，或复制你自己的现有角色目录为新目录，例如 `characters/my_character`。
2. 替换素材，修改新目录的 `character.json` 和 `persona.json`。`id` 使用不同值；素材裁切坐标必须与新图一致。
3. 关闭启动器后重新打开，新角色将出现在皮肤列表。选择并点击「启动桌宠」即可。
4. 也可通过根目录 `runner.json` 指定启动器默认选中的角色：

```json
{
  "schema_version": 1,
  "character": "characters/my_character"
}
```

也可以通过命令行预选角色。打开后仍然是启动器，点击「启动桌宠」才会创建桌宠并记住选择：

```powershell
.\DesktopPet.exe --character "characters/my_character"
```

角色目录也可以填写绝对路径。相对目录相对于 EXE 所在文件夹，不受当前命令行工作目录影响。程序保持单实例；已有实例时，再次运行只唤起已有启动器，命令行角色参数不会替换运行中的桌宠。若需预选外部目录，请先关闭原启动器。

修改 `character.json` 或图片后，通过启动器重新启动桌宠以加载新素材；人设支持下一次聊天自动重读。编辑器会保存六个人设字段，并保留原有人设和角色配置中的未知顶层扩展字段。编辑图片采用新的独立素材文件，保留原图、未修改的表情映射、尺寸配置与固定互动短句；新图片可通过单个 sprite 的 `remove_dark_background: false` 覆盖旧角色包全局去黑底行为，避免误删黑色轮廓。

## 验证与分享

普通回归和模拟 API 测试方法见 `使用说明.md`。新增测试覆盖另一个目录的默认尺寸/动作、单张透明 PNG、无效角色包、人设重读、历史切换和错误恢复。真实人设对比脚本使用临时角色副本，不修改当前角色文件。

分享角色时只分享对应角色文件夹即可。分享运行器时保留 EXE、`runner.json` 和角色文件夹。API Key 仍在现有「设置模型」界面填写，使用当前 Windows 账户加密保存；不要将 `model-settings.json` 或 `.env` 放入分享包。Git 忽略规则已保留，本次没有上传或推送。
